using UnityEngine;
using System.Collections;

public class buildertest : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Builder.cubes (new Vector3(1,1,1));
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
